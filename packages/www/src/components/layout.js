import React from "react";
import Header from "./header";
import { Container } from "theme-ui";

const Layout = () => {
  return (
    <Container>
      <Header />
    </Container>
  );
};

export default Layout;
